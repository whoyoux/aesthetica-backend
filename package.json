{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.7.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^3.0.9"
  }
}